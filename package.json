{
  "name": "djezzy-data-nexus",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack telecom infrastructure decision-support platform for site health, incidents, tickets, maintenance, and digital twin workflows.",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "db:up": "docker compose up -d db",
    "db:push": "npm --prefix backend run db:push",
    "db:seed": "npm --prefix backend run db:seed",
    "backend:dev": "npm --prefix backend run dev",
    "backend:typecheck": "npm --prefix backend run typecheck",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:lint": "npm --prefix frontend run lint",
    "frontend:build": "npm --prefix frontend run build",
    "scada:test": "cd scada_expert_system && python3 test_rules.py",
    "check": "npm run backend:typecheck && npm run frontend:lint && npm run frontend:build",
    "verify": "npm run check && npm run scada:test"
  }
}
